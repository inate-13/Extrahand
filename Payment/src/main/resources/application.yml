server:
  port: 8888
spring:
  application:
    name: Payment
  datasource:
    url: jdbc:mysql://localhost:3306/extrahand?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect


